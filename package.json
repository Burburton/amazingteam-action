{
  "name": "amazingteam-action",
  "version": "3.0.19",
  "private": true,
  "description": "GitHub Action for AmazingTeam Foundation",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
